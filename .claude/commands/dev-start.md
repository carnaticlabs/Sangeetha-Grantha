Start the full development stack (Database + Backend + Frontend).

Run: `make dev`

This will:
1. Start the PostgreSQL database on port 5432
2. Wait for the database to be healthy
3. Run the backend in dev mode on port 8080
4. Start the frontend on port 5001
5. Verify all services are running and healthy
