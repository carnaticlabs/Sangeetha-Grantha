Run the end-to-end steel thread test.

Run: `make steel-thread`

This performs: DB reset → migrate → seed with test_data.json → admin auth → sangita creation → participant bulk import → dashboard and payment queries.

Report the result and any failures.
