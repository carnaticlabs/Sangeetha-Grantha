Run all project tests and report results.

1. Backend tests: `make test`
2. Frontend tests: `make test-frontend`

Report a summary of pass/fail for each module.

For full stack validation, consider running `make steel-thread` which performs an end-to-end smoke test.
