# Frontend Environment Variables
# This file is a template for config/development.env
# The bootstrap script will copy this file to config/development.env if it doesn't exist.
#
# Note: config/development.env is gitignored. Do not commit sensitive values.

# Frontend API Configuration
VITE_API_BASE_URL=http://localhost:8080

# Backend API Configuration
API_HOST=0.0.0.0
API_PORT=8080
ADMIN_TOKEN=dev-admin-token
TOKEN_TTL_SECONDS=3600

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=sangita_grantha
DB_USER=postgres
DB_PASSWORD=postgres
DB_SCHEMA=public

# Storage Configuration
STORAGE_UPLOAD_DIR=uploads
STORAGE_PUBLIC_URL=http://localhost:8080/uploads

# Gemini AI Configuration (optional - add your API key if needed)
# SG_GEMINI_API_KEY=your-api-key-here
