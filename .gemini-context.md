# Gemini Context — Sangita Grantha

> **Canonical source of truth: [`CLAUDE.md`](CLAUDE.md).** Tech stack, architecture,
> the migration engine, audit-log and commit rules, and ports all live there — read it first.
> The full Gemini entrypoint — "Sangita Grantha Architect" persona, Gemini AI-service
> integrations, musicological domain rules, response style — is [`GEMINI.md`](GEMINI.md).
> This file is a thin pointer on purpose: it does **not** restate shared rules, so it can't
> drift (it previously told agents to avoid Flyway and use a Rust CLI — both now wrong).

## Current facts (authoritative text in CLAUDE.md)
- **Migrations:** **Flyway** only, via `make migrate` / `make db-reset` (ADR-013). Never Liquibase, never a custom runner. The old Rust CLI is archived (`tools/sangita-cli-archived/`); the interim Python `db-migrate` is superseded.
- **DB access:** always inside `DatabaseFactory.dbQuery { }`; never leak Exposed entities — return `@Serializable` DTOs.
- **Audit:** every mutation writes to `AUDIT_LOG`.
- **Commits:** include a `Ref: application_documentation/...` line.
- **Stack versions:** see [Current Versions](application_documentation/00-meta/current-versions.md) (PostgreSQL 18, Kotlin/Ktor/Exposed, React 19 + TypeScript 5.9). Don't hardcode versions here.
